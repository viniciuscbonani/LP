/*#include <stdio.h>
int main(){
  float n1, n2, opt, med;
  printf("Nota 1: ");
  scanf("%f", &n1);
  printf("Nota 2: ");
  scanf("%f", &n2);
  printf("Nota Prova Optativa: ");
  scanf("%f", &opt);
  if (n1 < opt)
    n1 = opt;
  else
    if (n2 < opt)
      n2 = opt;
  med = (n1 + n2)/2;
  if (med >= 6)
    printf("Aprovado com nota %.1f", med);
  else
    if (med < 3)
    printf("Reprovado com nota %.1f", med);
    else
      printf("De exame com nota de %.1f", med);
}*/ 